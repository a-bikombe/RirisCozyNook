{
    "buildCommand": "ng build --configuration production",
    "outputDirectory": "dist/riris-cozy-nook/browser",
    "functions": {
        "api/**/*.ts": {
            "runtime": "edge"
        },
        "api/**/*.js": {
            "runtime": "edge"
        }
    },
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/$1"
        },
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}