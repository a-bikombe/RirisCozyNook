{
    "buildCommand": "ng build --configuration production",
    "outputDirectory": "dist/browser/riris-cozy-nook",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/$1"
        },
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}