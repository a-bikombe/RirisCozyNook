{
    "buildCommand": "ng build --configuration production",
    "outputDirectory": "dist/riris-cozy-nook",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/$1"
        },
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}