{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run App",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/backend/index.js",
            "preLaunchTask": "Run App",
            "skipFiles": [
                "<node_internals>/**"
            ]
        }
    ]
}
